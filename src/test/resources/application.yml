spring:
#  datasource:
#원래 Test에서는 memory 모드로 돌린다고 다음과 같은 설정을 해야하는데
#Spring에서는 이걸 다 생략하면 memory 모드로 돌도록 만들어줌
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
##        show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug